<Window Name="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Analyzer" Width="1200" Height="740" MinWidth="665" MinHeight="160">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="330"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="330"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель кнопок -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                <Button Name="btnInsert" Content="Вставить" Margin="5" Padding="10,5" Background="Gray"/>
                <Button Name="btnScan" Content="Анализировать" Margin="5" Padding="10,5" Background="LightGreen"/>
                <Button Name="btnClear" Content="Стереть" Margin="5" Padding="10,5" Background="IndianRed"/>
                <Button Name="btnSend" Content="Отправить" Margin="5" Padding="10,5" Background="LightBlue"/>
                <ComboBox Name="cmbTextType" Width="180" Margin="5" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Текстовое поле для ввода -->
            <TextBox Name="textBox"
                     Grid.Row="1"
                     Margin="5"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="14"
                     Text="Введите текст здесь..."/>
        </Grid>

        <!-- Разделитель -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Gray"/>

        <!-- Правая панель -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Верхняя часть правой панели - заголовок -->
            <TextBlock Grid.Row="0" Text="Распределение длин слов" FontWeight="Bold" FontSize="16" Margin="10"/>

            <!-- Canvas для графиков -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10,0,10,10">
                <StackPanel Orientation="Vertical">

                    <TextBlock Text="Длины слов" FontWeight="Bold" Margin="0,5"/>
                    <Canvas Name="wordLengthChart" Height="250" Background="#f8f8f8" Margin="0,0,0,10"/>

                    <TextBlock Text="Лексическое разнообразие" FontWeight="Bold" Margin="0,5"/>
                    <Canvas Name="lexicalDiversityChart" Height="250" Background="#f8f8f8" Margin="0,0,0,10"/>

                    <TextBlock Text="Частота местоимений" FontWeight="Bold" Margin="0,5"/>
                    <Canvas Name="pronounChart" Height="250" Background="#f8f8f8" Margin="0,0,0,10"/>

                    <TextBlock Text="Уникальность слов" FontWeight="Bold" Margin="0,5"/>
                    <Canvas Name="uniquenessChart" Height="250" Background="#f8f8f8" Margin="0,0,0,10"/>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>